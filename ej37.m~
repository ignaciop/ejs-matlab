k = 1;
w0 = sqrt(k);

t = 0:0.1:10;
y = @(R) exp(-R*t./2).*sin(sqrt(w0^2 - R^2/4)*t);

colors = [b g r m k];

for R = 0:0.5:2
    yy = y(R);
    c = colors(randi(5,1));
    cs = num2str(c);
    hold on
    plot(t,yy,'m-')
    axis auto
    grid on
end

hold off